# FlashMind Docs — Orientação para Agentes

## Diretrizes do Repositório de Documentação

- Mantenha a separação entre `dominios/`, `guias/`, `models/`, `clientes/` e `ui/`.
- Todos os arquivos criados devem usar tags semânticas no frontmatter (ex: `#router/screen`, `#model/flutter`).
- Nunca crie wikilinks para notas inexistentes; valide sempre com `python3 _gerador/scan_app.py`.
